if (!/Mobile|iP(hone|od|ad)|Android|BlackBerry|IEMobile|Kindle|NetFront|Silk-Accelerated|(hpw|web)OS|Fennec|Minimo|Opera M(obi|ini)|Blazer|Dolfin|Dolphin|Skyfire|Zune/i.test(navigator.userAgent)) {

    var mobsl = document.getElementById("mobsl");

    if (mobsl) {
        mobsl.remove();
    }

    // слайдер пк
    // Для главной страницы


var tstScrOne = document.getElementsByClassName('splide_2');
if (tstScrOne.length !== 0) {

    if (/^\/$/g.test(location.pathname)) {
        document.addEventListener("DOMContentLoaded", function() {
            new Splide('.splide_2', {
                preloadPages: 1,
                type: 'loop',
                pagination: false,
                autoplay: true,
                rewind: true,
                pauseOnHover: false,
                pauseOnFocus: false,


            }).mount();
        }, false);
    }

}



}


// проверяем: если мобильные то показываем блок
if (/Mobile|iP(hone|od|ad)|Android|BlackBerry|IEMobile|Kindle|NetFront|Silk-Accelerated|(hpw|web)OS|Fennec|Minimo|Opera M(obi|ini)|Blazer|Dolfin|Dolphin|Skyfire|Zune/i.test(navigator.userAgent)) {

    var pcId = document.getElementById("pcId");

    if (pcId) {
        pcId.remove();
    }

    // Для главной страницы
    // слайдер мобильный внутренний
    // слайдер мобильный
    if (/^\/$/g.test(location.pathname)) {
        document.addEventListener("DOMContentLoaded", function() {


var tstScrTwo = document.getElementsByClassName('splide_vnutr');
if (tstScrTwo.length !== 0) {

            new Splide('.splide_vnutr', {
                type: 'loop',
                drag: false,
                focus: 'center',
                perPage: 2,
                pagination: false,
                arrows: false,
                autoScroll: {
                    speed: 0.5,
                },
            }).mount(window.splide.Extensions);

}



var tstScrTre = document.getElementsByClassName('splide_mobile');


if (tstScrTre.length !== 0) {

            new Splide('.splide_mobile', {
                preloadPages: 1,
                type: 'loop',
                pagination: false,
                autoplay: true,
                rewind: true,
                pauseOnHover: false,
                pauseOnFocus: false,
            }).mount();

}
        }, false);
    }

}
